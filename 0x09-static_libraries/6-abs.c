#include "main.h"

/**
 * _abs - function that compute the absolute value
 *
 * @n: intger that will be computed
 *
 * Return: absolute value of intger
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
