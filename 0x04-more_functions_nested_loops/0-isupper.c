#include "main.h"

/**
 * _isupper - a function that checks for uppercase character.
 *
 * Return: 1 if c is uppercase, 0 otherwise.
 */

int _isupper(int c)
{
	if (c >= 'A', c <= 'Z', c++)
		
		return (1);
	else if
		return (0);
}
